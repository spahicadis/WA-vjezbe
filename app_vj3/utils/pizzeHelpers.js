//U helper funkciju provjera sadrzaja i velicina arr
/*
  const allowedKeys = ["naziv", "cijena"];

  for (const [key, value] of Object.entries(req.body)) {
    const check = allowedKeys.some((allowedKeys) => allowedKeys === key);
    if (!check) {
      return res.status(400).json({ message: "Ključevi nisu dobri." });
    }
  }*/
